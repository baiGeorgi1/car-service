import { Car } from "./car";

export interface User {
    _id: string;
    username: string;
    email: string;
    password: string;
}
export interface UserAuth {
    _id: string;
    username: string;
    email: string;
    password: string;
}
